import 'package:flutter/material.dart';

class AppIcon {
  IconButton iconDelete = IconButton(
    onPressed: () {},
    icon: const Icon(Icons.delete),
    color: const Color.fromARGB(170, 80, 88, 245),
  );

  Icon iconFolder = const Icon(
    Icons.folder,
  );
  Icon iconSetting = const Icon(
    Icons.settings,
  );
  Icon iconAnalysis = const Icon(
    Icons.auto_graph_sharp,
  );
}
